{
  "comment": "Minimal event payload for the push/commit-analysis path. index.js reads this file but only inspects event.action / pull_request for pull_request events, so an empty object is sufficient for push."
}
